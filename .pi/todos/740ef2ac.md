{
  "id": "740ef2ac",
  "title": "Switch to Pi.dev + Zen Kimi K2.6, fix auth, add deployment smoke test",
  "tags": [
    "deployment",
    "auth",
    "model-switch",
    "pi.dev",
    "smoke-test"
  ],
  "status": "open",
  "created_at": "2026-05-16T10:03:00.066Z"
}

## Phase 1: Model Switch (Pi.dev + trader-zen/kimi-k2.6)
- [ ] Switch `config/agent_runtime.json` from `trader-openai/*` to `trader-zen/kimi-k2.6`
- [ ] Verify `src/pi_runner.ts` maps `trader-zen` correctly to the `opencode` provider (for Pi.dev)
- [ ] Ensure `src/agent.ts` and `src/harness.ts` handle zen model naming
- [ ] Delete or remove `src/opencode_runner.ts` references if dead code
- [ ] Run unit tests after model switch

## Phase 2: Enable Google OAuth for Website + APIs
- [ ] Uncomment/fix auth middleware in `src/web/server.ts` for all `/api/` routes
- [ ] Ensure `ALLOWED_USER_EMAIL` env var is used correctly
- [ ] Add test for authenticated vs unauthenticated API access
- [ ] Make sure dashboard login flow still works

## Phase 3: Build Deployment Smoke Test (Verify Pi.dev in Prod)
- [ ] Add `--dry-run` flag support to `src/agent.ts`
- [ ] Add `--dry-run` guard to `src/tools/alpaca_cli.ts` (no real orders submitted)
- [ ] Create `POST /api/test/hourly` and `POST /api/test/tactical` endpoints
- [ ] Create `GET /api/test/jobs/:id` endpoint for polling results
- [ ] Rate-limit test endpoints (max 1 concurrent per mode, max 3/hr)
- [ ] Add post-deployment smoke test that calls a dry-run tactical cycle and verifies Pi.dev completes successfully
- [ ] Ensure smoke test runs automatically as part of `scripts/deploy.sh`

## Phase 4: Deploy & Verify
- [ ] Commit changes
- [ ] Run all tests (`bun test`)
- [ ] Deploy to Cloud Run
- [ ] Trigger smoke test and verify Pi.dev with Zen Kimi K2.6 responds correctly
- [ ] Verify Google OAuth protects dashboard and APIs
