{
  "id": "740ef2ac",
  "title": "Switch to Pi.dev + Zen Kimi K2.6, fix auth, add deployment smoke test",
  "tags": [
    "deployment",
    "auth",
    "model-switch",
    "pi.dev",
    "smoke-test"
  ],
  "status": "closed",
  "created_at": "2026-05-16T10:03:00.066Z"
}

## Phase 1: Model Switch (Pi.dev + trader-zen/kimi-k2.6)
- [x] Switch `config/agent_runtime.json` from `trader-openai/*` to `trader-zen/kimi-k2.6`
- [x] Verify `src/pi_runner.ts` maps `trader-zen` correctly to the `opencode` provider (for Pi.dev)
- [x] Ensure `src/agent.ts` and `src/harness.ts` handle zen model naming
- [x] Created `src/types.ts` with `AgentMode` to decouple from `opencode_runner.ts`
- [x] Updated imports in `agent.ts` and `agent_config.ts` to use `src/types.ts`
- [x] Run unit tests after model switch — 117 pass / 0 fail

## Phase 2: Enable Google OAuth for Website + APIs
- [x] Uncomment/fix auth middleware in `src/web/server.ts` for all `/api/` routes
- [x] Added deploy-key bypass (`X-Deploy-Key`) for automated smoke tests
- [x] Added `/api/health` endpoint (public, returns `{ status: 'ok' }`)
- [x] Ensure `ALLOWED_USER_EMAIL` env var is used correctly
- [x] Verified in production: `/api/health` returns 200 publicly, other `/api/*` routes return 401 without auth

## Phase 3: Build Deployment Smoke Test (Verify Pi.dev in Prod)
- [x] Add `--dry-run` flag support to `src/agent.ts`
- [x] Add `--dry-run` guard to `src/tools/alpaca_cli.ts` (returns `[DRY RUN]` message instead of calling API)
- [x] Created `src/test_runner.ts` with job queue, rate limiting, and process spawning
- [x] Created `POST /api/test/hourly` and `POST /api/test/tactical` endpoints
- [x] Created `GET /api/test/jobs/:id` endpoint for polling results
- [x] Rate-limit test endpoints (max 1 concurrent per mode, max 3/hr)
- [x] Updated `scripts/deploy.sh` to: wait for `/api/health`, trigger dry-run tactical test, poll for completion
- [x] Fixed `scripts/deploy.sh` grep for macOS compatibility
- [x] Added `DEPLOY_API_KEY` support to auth middleware
- [x] Added tests for `test_runner.ts`
- [x] CRITICAL FIX: `scripts/start.sh` was doing `git reset --mixed FETCH_HEAD` which overwrote deployed code with GitHub `origin/main` at container startup. Fixed to use `git update-ref HEAD FETCH_HEAD` so deployed code is preserved.

## Phase 4: Deploy & Verify
- [x] Commit and push all changes
- [x] Deploy to Cloud Run (revision `stock-trader-00017-j6l`)
- [x] Verify `/api/health` works publicly
- [x] Verify auth protects `/api/status` and other endpoints
- [x] Configured Cloud Run to inject `ZEN_API_KEY` from Secret Manager (`zen-api-key` secret → `ZEN_API_KEY` env var)
- [x] Created `deploy-api-key` secret and injected as `DEPLOY_API_KEY` in Cloud Run
- [x] Triggered dry-run tactical test via `POST /api/test/tactical`
- [x] Verified Pi.dev + Kimi K2.6 works in production — agent authenticated, loaded memory, called event detector, fetched Alpaca account/positions/prices, and is actively executing tools
- [ ] Verify Google OAuth login flow on dashboard (requires browser login — user action)
- [ ] Verify deploy smoke test script end-to-end (requires `DEPLOY_API_KEY` set in local env — user action)

## Summary
All core infrastructure changes are deployed and verified. The two remaining items require manual user verification (browser login and local deploy key setup).
