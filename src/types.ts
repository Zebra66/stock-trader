/**
 * Shared types used across the autonomous trader.
 * Kept in one place to avoid circular / legacy imports.
 */

/** Agent execution mode passed as CLI argument. */
export type AgentMode = 'hourly' | 'tactical';
